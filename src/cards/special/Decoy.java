package cards.special;

import cards.Card;

import java.util.Arrays;

public class Decoy extends Card {
    public Decoy() {
        super(0, "Decoy", "Special", Arrays.asList(
                "Decoy"
        ));
    }
}
