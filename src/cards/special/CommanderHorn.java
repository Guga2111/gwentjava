package cards.special;

import cards.Card;

import java.util.Arrays;

public class CommanderHorn extends Card {
    public CommanderHorn() {
        super(0, "Commander Horn", "Special", Arrays.asList(
                "CommanderHorn"
        ));
    }
}
