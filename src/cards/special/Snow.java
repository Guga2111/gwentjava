package cards.special;

import cards.Card;

import java.util.Arrays;

public class Snow extends Card {
    public Snow() {
        super(0, "Snow", "Climate", Arrays.asList(
                "Snow"
        ));
    }
}
