package cards.special;

import cards.Card;

import java.util.Arrays;

public class Fog extends Card {
    public Fog() {
        super(0, "Fog", "Climate", Arrays.asList(
                "Fog"
        ));
    }
}
