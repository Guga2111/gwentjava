package cards.special;

import cards.Card;

import java.util.Arrays;

public class Scorch extends Card {
    public Scorch() {
        super(0, "Scorch", "Special", Arrays.asList(
                "Scorch"
        ));
    }
}
