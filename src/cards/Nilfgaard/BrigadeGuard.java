package cards.Nilfgaard;

import cards.Card;

import java.util.Arrays;

public class BrigadeGuard extends Card {
    public BrigadeGuard() {
        super(3, "Impera Brigade Guard", "Infantry", Arrays.asList(
                "Tight Bond"
        ));
    }
}
