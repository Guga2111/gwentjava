package cards.NorthernRealms;

import cards.Card;

import java.util.Arrays;

public class Dijkstra extends Card {
    public Dijkstra() {
        super(4, "Sigismund Dijkstra", "Artillary", Arrays.asList(
                "Spy"
        ));
    }
}
