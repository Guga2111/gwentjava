package cards.NorthernRealms;

import cards.Card;

import java.util.Arrays;

public class Thaler extends Card {
    public Thaler() {
        super(1, "Thaler", "Siege", Arrays.asList(
                "Spy"
        ));
    }
}
