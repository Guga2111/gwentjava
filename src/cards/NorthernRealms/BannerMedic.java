package cards.NorthernRealms;

import cards.Card;

import java.util.Arrays;

public class BannerMedic extends Card {
    public BannerMedic() {
        super(5, "Dun Banner Medic", "Siege",Arrays.asList(
                "Heal"
        ));
    }
}
