package cards.NorthernRealms;

import cards.Card;

import java.util.Arrays;

public class Dethmold extends Card {
    public Dethmold(){
        super(6, "Dethmold", "Artillary", Arrays.asList(
                "Basic"
        ));
    }
}
