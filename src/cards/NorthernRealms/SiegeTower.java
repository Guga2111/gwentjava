package cards.NorthernRealms;

import cards.Card;

import java.util.Arrays;

public class SiegeTower extends Card {
    public SiegeTower(){
        super(6, "Torre de Cerco", "Siege", Arrays.asList(
                "Basic"
        ));
    }
}
