package cards.scoiatel;

import cards.Card;

import java.util.Arrays;

public class Riordain extends Card {
    public Riordain() {
        super(1, "Riordain", "Artillary", Arrays.asList(
                "Basic"
        ));
    }
}
