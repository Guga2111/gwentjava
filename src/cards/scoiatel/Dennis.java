package cards.scoiatel;

import cards.Card;

import java.util.Arrays;

public class Dennis extends Card {
    public Dennis() {
        super(6, "Dennis Cranmer", "Infantry", Arrays.asList(
                "Basic"
        ));
    }
}
